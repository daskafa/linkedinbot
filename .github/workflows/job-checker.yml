name: LinkedIn Job Checker

on:
  schedule:
    # Her 4 saatte bir çalışır (00:00, 04:00, 08:00, 12:00, 16:00, 20:00 UTC)
    - cron: '0 */4 * * *'
  
  # Manuel çalıştırma için
  workflow_dispatch:

jobs:
  check-jobs:
    runs-on: ubuntu-latest
    
    steps:
    - name: Checkout repository
      uses: actions/checkout@v4
      with:
        token: ${{ secrets.GITHUB_TOKEN }}
    
    - name: Set up Python
      uses: actions/setup-python@v5
      with:
        python-version: '3.11'
    
    - name: Install dependencies
      run: |
        pip install -r requirements.txt
    
    - name: Run job scraper
      env:
        TELEGRAM_TOKEN: ${{ secrets.TELEGRAM_TOKEN }}
        CHAT_ID: ${{ secrets.CHAT_ID }}
        LINKEDIN_SEARCH_URLS: ${{ secrets.LINKEDIN_SEARCH_URLS }}
        FILTER_KEYWORDS: ${{ secrets.FILTER_KEYWORDS }}
      run: |
        python linkedin_job_scraper.py
    
    - name: Commit seen jobs
      run: |
        git config --local user.email "github-actions[bot]@users.noreply.github.com"
        git config --local user.name "github-actions[bot]"
        git add seen_jobs.json
        git diff --quiet && git diff --staged --quiet || git commit -m "Update seen jobs [skip ci]"
        git push
